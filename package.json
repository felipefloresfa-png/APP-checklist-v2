{
  "name": "app-checklist",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp -r components services *.html *.tsx *.ts dist && printf '%s\\n' '/*.ts' '  Content-Type: text/javascript' '/*.tsx' '  Content-Type: text/javascript' '/components/*.tsx' '  Content-Type: text/javascript' '/services/*.ts' '  Content-Type: text/javascript' > dist/_headers"
  }
}