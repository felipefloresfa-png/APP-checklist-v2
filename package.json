{
  "name": "app-checklist",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp -r components services *.html *.tsx *.ts *.json dist && (echo '/**/*.ts' && echo '  Content-Type: text/javascript' && echo '/**/*.tsx' && echo '  Content-Type: text/javascript') > dist/_headers"
  }
}